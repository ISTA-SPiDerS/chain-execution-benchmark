# Example benchmark

In the example benchmark we generate the transaction parameters for 'num_tx' transactions for the workloads [here](../workloads).
First, it loads all the data structures from their respective files, constructs the weighted distribution vectors and then prints the transaction parameters for the respective workloads.